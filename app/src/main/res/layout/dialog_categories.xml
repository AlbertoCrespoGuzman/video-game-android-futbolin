<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <TextView
        android:layout_marginTop="@dimen/margin_sm"
        android:id="@+id/dialog_categories_title"
        android:textSize="@dimen/text_size_l"
        android:text="@string/strategies"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/dialog_categories_table"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
<TableLayout
    android:layout_marginTop="@dimen/margin_sm"
    android:id="@+id/dialog_categories_table"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toBottomOf="@+id/dialog_categories_title"
    app:layout_constraintBottom_toTopOf="@+id/dialog_categories_close_button"
    android:paddingRight="@dimen/margin_n"
    android:paddingLeft="@dimen/margin_n"
    android:stretchColumns="*"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

</TableLayout>
    <Button
        android:layout_margin="@dimen/margin_sm"
        android:id="@+id/dialog_categories_close_button"
app:layout_constraintTop_toBottomOf="@+id/dialog_categories_table"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/close"
        android:background="@color/futbolinAzul"
        android:textColor="#FFFFFF"/>
</androidx.constraintlayout.widget.ConstraintLayout>